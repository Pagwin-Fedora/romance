true_love:
  steps:
    - name: repo?
      container: busybox
      cmd: pwd && ls && ls --all
      env:
    - name: build
      container: rust:buster
      cmd: cargo build --release
      env: 
    - name: env_check
      container: busybox
      cmd: echo $HELLO
      env:
        HELLO: World!
